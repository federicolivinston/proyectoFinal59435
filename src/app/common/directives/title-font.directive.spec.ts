import { ElementRef } from '@angular/core';
import { TitleFontDirective } from './title-font.directive';

describe('TitleFontDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleFontDirective();
    expect(directive).toBeTruthy();
  });
});
